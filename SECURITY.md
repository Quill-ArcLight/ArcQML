# 安全问题报告与预览版已知限制

请私下联系 quill@arclightquantum.com，维护人 liuxl，提供版本、平台、复现步骤和影响范围。

本次为 Windows 实验性预览版，内存安全问题 S01 尚未修复：公开的低层 safe Rust 接口接收不携带生命周期的裸指针描述符；如果指针所指矩阵或相关缓冲区在 Runtime 使用前被释放，可能发生无效内存访问、崩溃、错误计算或其他未定义行为。目前结论来自静态接口审查，未证明可远程利用。

直接使用底层接口时，调用方必须保证所有指针有效、类型/长度/对齐正确，且底层内存在最后一次相关 Runtime 调用完成前持续有效。普通应用优先采用高层接口；这不是对高层接口不存在缺陷的安全保证。使用说明只能降低误用概率，不能修复接口缺陷。

此预览版用于可信的本地教学和实验，暂不推荐生产环境或不可信插件。功能测试通过不等于内存安全验收通过。S01 保留为下一个补丁版本的优先修复项，不承诺尚未确定的日期。

维护者会核实报告并协调修复及披露时间，目前不承诺固定响应时限。请勿在公开 issue 中提交凭据或其他敏感数据。
